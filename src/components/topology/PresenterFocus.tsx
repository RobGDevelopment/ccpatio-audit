"use client";

/**
 * Presenter focus previously auto-fitView'd on each dwell.
 * Disabled so Play / Movie Mode leave the viewport under manual control.
 */
export function PresenterFocus() {
  return null;
}

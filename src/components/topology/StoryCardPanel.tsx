"use client";

/**
 * Bottom story ticker — intentionally disabled for executive grid focus.
 * Re-enable later by restoring the previous StoryCardPanel implementation.
 */
export function StoryCardPanel() {
  return null;
}

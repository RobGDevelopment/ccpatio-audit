/**
 * @deprecated HTML overlay removed — GHL rail is now RF nodes in humanCanvasLayout.
 * Re-exports canonical pipeline defs for any remaining consumers.
 */
export { GHL_PIPELINES, ghlChipId } from "./ghlPipelines";

export function GhlPipelineRail() {
  return null;
}
